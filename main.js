import './style.css'

class User {
  constructor(firstName, lastName, posts, comments, likes) {
    this.firstName = firstName
    this.posts = posts
    this.comments = comments
    this.likes = likes
  }
}